Для стилизации элементов используется следующая конструкция:
style.
    .class {
        color: red;
    }
После готовый класс подключается к элементу:
button(class="class")

Стили подключенные таким образом имеют приоритет перед остальными.